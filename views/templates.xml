<odoo>
  <data>
    <template id="jobs_page" name="Open Positions">
      <t t-call="website.layout">
        <div id="wrap">
          <div class="container py-5">
            <h1 class="mb-4">Open Positions</h1>
            <div class="list-group">
              <t t-if="not jobs">
                <div class="alert alert-info">
                  Currently, there are no open positions. Please check back later!
                </div>
              </t>
              <t t-foreach="jobs" t-as="job">
                <a t-attf-href="/jobs/detail/{{job.id}}"
                  class="list-group-item list-group-item-action flex-column align-items-start mb-3 border rounded">
                  <div class="d-flex w-100 justify-content-between">
                    <h5 class="mb-1" t-field="job.name" />
                    <small><t t-esc="job.no_of_new_application" /> New Applications</small>
                  </div>
                  <p class="mb-1"> Location: <span t-field="job.address_id.city" />
                  </p>
                </a>
              </t>
            </div>
          </div>
        </div>
      </t>
    </template>
  </data>
</odoo>
